# File Explorer (Console) - Capstone Project

Simple interactive file explorer written in C++17 using std::filesystem.

## Features
- ls, pwd, cd, cat, touch, mkdir, rm (-r), cp (recursive), mv, search, chmod, info
- Console interactive shell

## Build (WSL / Ubuntu)
```bash
make
./file_explorer