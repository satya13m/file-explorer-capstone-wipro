# Day 5: Add file permission management features

Enhanced the file explorer with advanced features:
- `chmod` → change file / folder permissions.
- `history` → show previously typed commands.
- `clear` → clear the console for a clean view.
- `config.txt` → lets users customize UI colors.

